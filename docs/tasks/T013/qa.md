# QA verdict — T013 (MZ 비주얼 리디자인)

All evidence below was generated by QA this session under
`docs/tasks/T013/evidence/qa/`. The developer's numbers were NOT trusted; every
layer was re-run from a clean state and the document audited claim-by-claim.

## Independent test results
- Format:      evidence/qa/format.txt — "41 files (0 changed)", EXIT_CODE=0
- Analyze:     evidence/qa/analyze.txt — "No issues found!", EXIT_CODE=0
- Unit:        evidence/qa/test-unit.txt — +143 passed, 0 failed, EXIT_CODE=0
- Widget:      evidence/qa/test-widget.txt — +42 passed, 0 failed, EXIT_CODE=0
- Golden:      evidence/qa/test-golden.txt — +2 passed on a CLEAN run (`flutter test --tags golden`, no `--update-goldens`), EXIT_CODE=0
- Full suite:  evidence/qa/test-all.txt — +186 passed, 0 failed, EXIT_CODE=0
- Coverage:    evidence/qa/coverage.txt — 95.7% overall (no project threshold defined); all T013-touched files high: rgb_wheel 100%, switch_menu 100%, keycap 100%, home_screen 100%, stats_panel 98.6%, settings_sheet 98.4%
- Integration: not applicable — no `integration_test/` suite in repo; runtime smoke (below) covers on-device behavior
- Build:       evidence/qa/build.txt — `build/app/outputs/flutter-apk/app-debug.apk`, ls -l = 154,383,712 bytes (evidence/qa/apk-badging.txt), EXIT_CODE=0
- Smoke:       evidence/qa/smoke/screenshot-1.png + screenshot-2.png + logcat.txt — installed+launched `com.geontoybox.cliker/.MainActivity` on emulator-5554 (am-start.txt "Status: ok"); fatal-scan.txt: FATAL EXCEPTION=0, E/flutter=0, AndroidRuntime=0; UI renders the MZ layout and count went 0 → 5 on five taps with live LED ripples

## Document audit
evidence/qa/doc-audit.txt — 19 claims audited, ALL BACKED. 0 UNBACKED, 0 CONTRADICTED.
- Code claims verified at the cited `file:LINE` in the working tree (read this session).
- Result claims re-run independently into `evidence/qa/`; counts/exit codes match exactly.
- `git diff --stat` reconciles exactly: 13 tracked files (899 ins / 441 del) + 4 untracked new files (208+237+89+91 = 625 lines) = the claimed 17 files, 1524 insertions, 441 deletions. No undocumented changes.
- Test-integrity: `grep -rn "skip:" test/` → none; the affected tests were UPDATED/STRENGTHENED, not deleted or weakened (old switch-selection tests now open the menu first; old keycap tests carry the new required `stemColor`).

## Package rename verification
- evidence/qa/deps-and-rename.txt: `git grep secondsyndrome` → exit 1 (no source/build match). The string survives only inside `docs/tasks/T013/dev.md` (a known-limitations note about a stale emulator package — not a code reference).
- build.gradle.kts:23 `namespace = "com.geontoybox.cliker"`, :38 `applicationId = "com.geontoybox.cliker"`.
- evidence/qa/apk-badging.txt: aapt → `package: name='com.geontoybox.cliker'`, launchable `com.geontoybox.cliker.MainActivity`. The new id is what builds and launches.
- pubspec.yaml/lock NOT in the diff → 0 new package dependencies; deps remain flutter, cupertino_icons, flutter_riverpod, shared_preferences, audioplayers.

## Spec conformance (acceptance criteria)
- AC1 (chips no longer inline; `switch-menu-button` opens 11 `switch-chip-<id>`) → MET.
  smoke_widget_test (test-widget.txt) asserts all 11 chips absent at cold start, all 11 present after
  tapping the menu button (`SwitchCatalog.all` hasLength 11). home_screen.dart:43,136; switch_menu.dart:111-118,169.
- AC2 (menu tap sets `selectedSwitchId`, closes, keycap stemColor/label update) → MET.
  smoke_widget_test selects `red` and `speedSilver`: provider becomes that id, `SwitchMenu.sheetKey` gone,
  keycap label switches to the new `nameKo`. switch_menu.dart:134-135; home_screen.dart:91,93.
- AC3 (`RgbWheel` pick → `onColorChanged`, `ledColorArgb` updates + persists across a fresh
  ProviderContainer; keycap glow follows; self-painted, no new dep) → MET.
  settings_sheet_test AC3 (test-widget.txt) taps the home wheel, asserts the provider equals the exact
  `RgbWheel.colorForHue` emitted color, keycap glow RGB follows, and a fresh container over the same prefs
  keeps the pick. rgb_wheel_test (unit+widget) covers hueAt/colorForHue + tap/drag. RgbWheel is CustomPaint
  (rgb_wheel.dart:107-208), no color-picker package (deps-and-rename.txt).
- AC4 (exactly 2 stats `stat-total`,`stat-rpm`; increment on tap; reset confirm→0 persists) → MET.
  stats_panel_test asserts only the two keys exist (old stat-session/cpm/best absent), increment, and
  reset→confirm→0 persisted across a fresh container. Live smoke shows 0 → 5. stats_panel.dart:28,31,117.
- AC5 (Keycap requires `stemColor`; observable pressed state differs in travel/scale; onPressDown/onPressUp
  once each; goldens regenerated and pass CLEAN) → MET.
  keycap_test AC5 reads `KeycapSwitchPainter.stemColor` and confirms a different switch repaints with a
  different stem color; AC2 confirms `_pressScale` ≈1.0 at rest → <1.0 while held and each callback fires
  exactly once. keycap.dart:47,89,300-301,337-339. Goldens binary-changed and pass on `--tags golden`
  with NO `--update-goldens` (test-golden.txt).
- AC6 (analyze clean, format 0, all tests green, 0 new deps) → MET. format.txt, analyze.txt, test-all.txt,
  deps-and-rename.txt.
- AC7 (`flutter build apk --debug` succeeds; runtime smoke) → MET. build.txt + apk-badging.txt + smoke/.
- Scope (core tap→sound/haptic/stats loop, audioplayers mediaPlayer mode, 11-switch catalog intact) → MET.
  click_sound_player.dart NOT in the diff; `PlayerMode.mediaPlayer` at click_sound_player.dart:74;
  switch catalog still 11 ids (blue, brown, red, black, white, gray, clear, silentRed, silentBlack,
  speedSilver, darkGray); core loop verified live (smoke) and in smoke_widget_test.

## Findings
None. Every applicable test-pyramid layer passed with QA-generated evidence, the
document audit is fully BACKED (19/19), the package rename builds and launches as
`com.geontoybox.cliker` with no `secondsyndrome` left in source, no new package
dependency was added, and every acceptance criterion is met.

Note (not a defect): subjective "does it look MZ/cool" quality is the planner/user's
call on web; QA verified the objective ACs and the clean package-rename build.

## Verdict: PASS
