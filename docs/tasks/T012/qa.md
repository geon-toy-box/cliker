# QA verdict — T012 (스위치 11종 확장 + UI 개편)

## Verdict: PASS

All evidence below was generated by QA this session under
`docs/tasks/T012/evidence/qa/`, independent of the developer's subtree.

## Independent test results
- Format:    evidence/qa/format.txt — `Formatted 37 files (0 changed)`, EXIT_CODE=0
- Analyze:   evidence/qa/analyze.txt — `No issues found!`, EXIT_CODE=0
- Unit:      evidence/qa/test-unit.txt — `+132: All tests passed!`, EXIT_CODE=0
- Widget:    evidence/qa/test-widget.txt — `+36: All tests passed!` (non-golden), EXIT_CODE=0
- Golden:    evidence/qa/test-golden.txt — `+2: All tests passed!`, EXIT_CODE=0
             (run CLEAN, no `--update-goldens`)
- Full suite: evidence/qa/test-all.txt — `+171: All tests passed!`, EXIT_CODE=0
- Coverage:  N/A — T012 Test plan does not set a coverage threshold (spec §Test plan).
- Integration: N/A — spec marks integration N/A (existing flow preserved; press
             wiring covered by smoke_widget_test press-wiring case).
- Build:     evidence/qa/build.txt — `✓ Built build/app/outputs/flutter-apk/app-debug.apk`,
             EXIT_CODE=0; artifact on disk = 208,830,514 bytes (`ls -l` recorded).
- Determinism (AC2): evidence/qa/gen-sounds-determinism.txt — gen_sounds.py run
             twice, all 22 WAVs byte-identical (DIFF_RUN1_RUN2_EXIT=0) AND identical
             to the committed assets (DIFF_ORIG_RUN1_EXIT=0); EXIT 0 both runs.
- Smoke:     evidence/qa/smoke/ — installed on emulator-5554 (API 35), MainActivity
             RESUMED, "Fully drawn +1s699ms"; logcat.txt has NO FATAL / NO
             E/flutter / no app exception. screenshot-1.png (2-value stats +
             sculpted keycap + 11-chip selector), screenshot-2-after-taps.png
             (5 taps drove total 1,516→1,521, RPM 5), screenshot-3-chip.png
             (적축 chip tap → keycap label "Red" + chip highlighted "리니어 · 45cN").

No test suite is empty (lowest = golden at +2). No test file was deleted
(git diff --diff-filter=D HEAD empty; 18 dart test files before and after);
catalog/stats/smoke tests were UPDATED, not removed.

## Document audit
evidence/qa/doc-audit.txt — every result/code/test claim BACKED and
independently reproduced; zero UNBACKED, zero CONTRADICTED *work* claims.
One narrative inaccuracy logged (not document-failing): dev.md self-audit states
"This is not a git repo" — it IS a git repo (`.git/` present, `git status`
works). This did not falsify any work claim: `git diff --stat HEAD` confirms
EXACTLY the 14 modified files dev.md lists + 14 untracked new WAVs, with no
undocumented source change, so the file-change list dev reached via `find` is
nonetheless correct and complete. Flagged for the developer to correct.

## Spec conformance (acceptance criteria)
- AC1 (11-switch catalog exact order/fields) → MET. lib/domain/switch_type.dart
  :255-267 lists the 11 in spec order; verified row-for-row against the spec
  table (kind/forceCn/stemColor/defaultLed/haptic) — all match. All stemColors
  reference AppColors tokens whose hex match spec (app_colors.dart:36-42, +
  app_colors_test asserts each ARGB). Every defaultLed ∈ ledPalette; every
  haptic ∈ (0,1]; defaultSwitch==all.first==blue (:270). switch_catalog_test
  asserts all 11 row-for-row → test-unit.txt +132.
- AC2 (22 valid WAVs + determinism) → MET. 22 WAVs on disk, all mono/44100/
  16-bit, all >1KB (validated each, gen-sounds-determinism.txt + header dump);
  re-run byte-identical x2 and == committed; switch_assets_test asserts exactly
  22 (test line 77 hasLength(22)).
- AC3 (exactly 2 stats, tap increments) → MET. stats_panel.dart:58-87 renders
  only stat-total + stat-rpm(=stats.cpm); old stat-session/stat-cpm/stat-best
  asserted absent by stats_panel_test + smoke_widget_test; runtime smoke showed
  total 1,516→1,521 and RPM 5 on taps.
- AC4 (reset → confirm dialog → zero + persist; cancel keeps) → MET.
  stats_panel.dart:93-122; stats_panel_test covers cancel-keeps,
  confirm-zeroes-and-persists (fresh container reads 0), barrier-dismiss-keeps.
- AC5 (keycap observable press + onPressDown/Up once + goldens) → MET.
  keycap.dart travel=size*0.075 (18px @ default ≥10px), topScale=1-0.07*depth;
  keycap_test asserts _pressScale ≈1.0 at rest, <1.0 while held, back to 1.0 on
  release, and one onPressDown/onPressUp per press; goldens pass on a CLEAN run
  (no --update-goldens) → test-golden.txt +2.
- AC6 (11 chips switch-chip-<id>, horizontal scroll, tap selects + relabels) →
  MET. home_screen.dart:122-138 builds all 11 chips at once in a horizontal
  SingleChildScrollView+Row; smoke_widget_test asserts all 11 chip keys
  findable + a right-axis Scrollable + red/speedSilver selection changes
  selectedSwitchId and keycap label; runtime smoke confirmed Red selection.
- AC7 (analyze/format/test green) → MET (see Independent test results).
- AC8 (apk debug build + smoke) → MET. Build EXIT 0 with artifact on disk;
  runtime smoke on emulator-5554 clean.

## Scope checks
- No new package deps: `git diff HEAD -- pubspec.yaml` empty. MET.
- Audio still PlayerMode.mediaPlayer: lib/audio/click_sound_player.dart:74
  `playerMode: PlayerMode.mediaPlayer` (NOT reverted to lowLatency). MET.
- Core tap→sound/haptic/stats loop intact: smoke_widget_test press-wiring case
  (down clip + 1 haptic + 1 click; up clip) passes in +171 suite. MET.

## Notes for the developer (non-blocking)
- dev.md §Self-audit incorrectly states "This is not a git repo." It is one;
  prefer `git diff --stat HEAD` over `find -newermt` next time. Your file list
  was correct regardless, so this does not block — but please correct the claim.
- Minor citation drift: dev.md cites keycap.dart:280-340 for travel/scale; the
  exact lines are 286-293 (within the cited build method). Cosmetic only.

VERIFIED
