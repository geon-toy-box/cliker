# QA verdict — T008

Stats panel (total · session · CPM · best) + reset. Independently verified by QA
on emulator-5554 (Android 15, API 35). All evidence under
`docs/tasks/T008/evidence/qa/` was generated by QA this session; the developer's
`evidence/dev/` was not reused.

## Verdict: PASS

## Independent test results
- Format:      evidence/qa/format.txt — `Formatted 34 files (0 changed)`, EXIT_CODE=0
- Analyze:     evidence/qa/analyze.txt — `No issues found! (ran in 1.6s)`, EXIT_CODE=0
- Unit:        evidence/qa/test-unit.txt — `+83: All tests passed!`, EXIT_CODE=0
               (incl. the 4 new `thousands` tests, progress lines +79…+82)
- Widget:      evidence/qa/test-widget.txt — `+37: All tests passed!`, EXIT_CODE=0
- Widget (stats_panel isolated): evidence/qa/test-widget-statspanel.txt —
               `+7: All tests passed!`, EXIT_CODE=0 (the 7 T008 widget tests)
- Full suite:  evidence/qa/test-full.txt — `+120: All tests passed!`, EXIT_CODE=0
               (83 unit + 37 widget; no skips, no failures; M1 keycap golden +83
               and T007 settings_sheet tests all green — no regression)
- Golden:      keycap golden (M1) ran green inside the full suite
               (evidence/qa/test-full.txt). No T008-specific golden — spec marks
               it 선택/optional; rendered strings are asserted by the widget tests.
- Coverage:    N/A — no coverage threshold defined for this task (spec).
- Integration: N/A — spec marks integration N/A (end-to-end is T006).
- Build:       evidence/qa/build.txt — `✓ Built build/app/outputs/flutter-apk/
               app-debug.apk`, EXIT_CODE=0. Artifact on disk:
               evidence/qa/build-artifact.txt — `app-debug.apk` 189,299,825 bytes.
- Smoke:       evidence/qa/smoke/screenshot-1…7.png + smoke/logcat.txt — app
               launches, panel + keycap + settings render, counters grow live,
               reset confirm zeroes; logcat FATAL EXCEPTION count = 0, no
               E/flutter or AndroidRuntime errors (only benign audioplayers
               MediaFocusControl audio-focus lines).

## Document audit
evidence/qa/doc-audit.txt — 13 claims extracted from dev.md and verified
claim-by-claim against the tree + re-run results. UNBACKED=0, CONTRADICTED=0;
all BACKED. File inventory line counts (22/191/193/30/250) match `wc -l`
exactly. `_StatsReadout`/`_StatTile` confirmed removed from home_screen.dart.
No undocumented behavioral change; pubspec deps unchanged (no new dep, no intl).

## Spec conformance (acceptance criteria)
- AC1 — panel shows total/session/CPM/best with labels, each value carries a
  findable Key (stat-total/-session/-cpm/-best) → **met**. Code:
  lib/widgets/stats_panel.dart:24-33,64-99; widget test asserts all four keys +
  labels present and cold-start 0 (test-widget-statspanel.txt +0). Runtime:
  evidence/qa/smoke/screenshot-1-initial.png (누적/세션/CPM/최고 CPM, all 0).
- AC2 — registering clicks increments total+session in the UI and thousands
  formatting applies (1,234) → **met**. Code: registerClick increments
  total/session (lib/providers/stats_providers.dart:103-120), panel formats via
  `thousands` (stats_panel.dart:67,75). Widget tests: one click→1/1, and seeding
  1233 + one click→`1,234` while session→1 (test-widget-statspanel.txt +2,+3).
  `thousands` unit tests cover grouping, multi-comma, AND negatives
  (test/unit/number_format_test.dart:6-29; test-unit.txt +79…+82). Runtime:
  screenshot-2-after-taps.png shows 12/12/12/12 after 12 live taps.
- AC3 — reset button → confirm dialog; cancel keeps values; confirm zeroes all
  four AND resets statsProvider persistently → **met**. Code: reset button →
  _confirmReset → AlertDialog; confirm calls resetStats() (stats_panel.dart:106,
  121-149), resetStats writes 0 to prefs (stats_providers.dart:123-128). Widget
  tests: cancel keeps total=5 + provider=5 (distinguishes cancel from reset — it
  would fail if cancel reset); confirm zeroes all four displayed + provider AND a
  fresh ProviderContainer over the same prefs reads totalClicks=0/bestCpm=0
  (genuine persistence — would read 7 if reset hadn't written prefs); barrier
  dismiss keeps values (test-widget-statspanel.txt +4,+5,+6). Runtime:
  screenshot-3 dialog, screenshot-4 cancel (values NOT zeroed),
  screenshot-6-reset-confirmed.png (all four → 0).
- AC4 — analyze `No issues found!`, format exit 0, `flutter test` green → **met**
  (evidence/qa/analyze.txt, format.txt, test-full.txt; all EXIT_CODE=0).
- AC5 — `flutter build apk --debug` succeeds → **met** (evidence/qa/build.txt
  EXIT_CODE=0; artifact 189,299,825 bytes on disk, build-artifact.txt).

## Scope checks
- No new package deps: pubspec.yaml deps = flutter_riverpod, shared_preferences,
  audioplayers, cupertino_icons (all M1/T007). No `intl`. (doc-audit omission
  check.)
- thousands formatting is a local helper (lib/util/number_format.dart), no intl.
- Stats logic NOT reimplemented: reset delegates to
  `statsProvider.notifier.resetStats()` (stats_panel.dart:148); StatsNotifier
  unchanged by this task.

## Regression check (M1 core loop + T007 settings)
- M1 keycap tap → sound/haptic/stats wiring intact: home_screen.dart:50-64
  handlePressDown plays sound, fires haptic, ref.read(statsProvider).
  registerClick; runtime screenshot-2 shows live stat growth on tap.
- Switch selector intact: home_screen.dart:96,111-193; runtime screenshot-1
  shows 청축/갈축/적축/흑축 chips, 청축 selected.
- Settings button intact and opens T007 sheet: home_screen.dart:74-83;
  runtime screenshot-7-settings-sheet.png (설정 / 사운드·햅틱 토글 / LED 색 /
  LED 모드).
- Stat Keys reused, not broken: home_screen re-exports stat-total/-session/-cpm
  (home_screen.dart:27-29); M1 smoke + T007 widget tests all pass in the full
  suite (test-full.txt +120, no regression).

## Findings
None. Every applicable layer passed with QA-generated evidence, the document
audit is fully BACKED, and every acceptance criterion (AC1–AC5) plus all scope
and regression checks are met.

## Verdict: PASS
